{
  "name": "fakenet",
  "version": "0.0.1",
  "description": "A fake bitcoin network running locally (uses RegTest mode of bitcoind). Defaults to BitcoinSV, but will probably work with other chains too",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Aleksandar Dinkov",
  "license": "ISC",
  "dependencies": {
    "bitcoin-core": "^3.0.0",
    "bsv": "^1.5.0",
    "rimraf": "^3.0.2"
  }
}
