{
  "name": "fakenet",
  "version": "0.0.3",
  "description": "A fake bitcoin network running locally (uses RegTest mode of bitcoind).",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Aleksandar Dinkov",
  "license": "LICENSE.md",
  "dependencies": {
    "bsv": "^1.5.0",
    "lock-taskqueue": "^1.0.1",
    "node-fetch": "^2.6.0",
    "rimraf": "^3.0.2"
  }
}
